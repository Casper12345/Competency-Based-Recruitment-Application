package model;

import java.util.Collections;
import java.util.Comparator;
import persistenceAPI.DataBaseConnection.Objects.JobDescriptionSkill;

/**
 * Created by Casper on 17/07/2017.
 */
public class Sort {

  /*
  public

  Collections.sort(d, new Comparator[JobDescriptionSkill] {
    override def compare(o1: JobDescriptionSkill, o2: JobDescriptionSkill): Int = {
    if(o1.skillID > o2.skillID){
      return 1
    }else if (o1.skillID < o2.skillID){
      return -1
    }else{
      return 0
    }
     }


        Collections.sort(d, new Comparator[JobDescriptionSkill] {
      override def compare(o1: JobDescriptionSkill, o2: JobDescriptionSkill): Int = {
        if(o1.skillID > o2.skillID){
         return 1
        }else if (o1.skillID < o2.skillID){
        return -1
      }else{
          return 0
        }
    }


*/


  }
